# QA_GURU_BELEVTSEVA_HW_6
## 🧠 Условные операторы,циклы и функции в Python

## 🔹 Условные операторы (`if / elif / else`)

Используются, чтобы программа **выполняла разные действия** в зависимости от условий.

```python
x = 5

if x > 0:
    print("Положительное число")
elif x == 0:
    print("Ноль")
else:
    print("Отрицательное число")
```

💡 **Запомни:**

* После `if`, `elif`, `else` — двоеточие `:`
* Отступы (4 пробела) обязательны
* Можно комбинировать условия с `and`, `or`, `not`

```python
age = 20
if age >= 18 and age < 60:
    print("Взрослый человек")
```

---

## 🔹 Операторы сравнения

| Оператор | Значение         | Пример   | Результат |
| -------- | ---------------- | -------- | --------- |
| `==`     | равно            | `5 == 5` | True      |
| `!=`     | не равно         | `5 != 3` | True      |
| `>`      | больше           | `10 > 2` | True      |
| `<`      | меньше           | `4 < 9`  | True      |
| `>=`     | больше или равно | `7 >= 7` | True      |
| `<=`     | меньше или равно | `3 <= 5` | True      |

---

## 🔹 Цикл `for`

Используется, чтобы **повторить действие несколько раз** или пройтись по элементам коллекции.

```python
for i in range(5):
    print(i)
```

➡️ Выведет:

```
0
1
2
3
4
```

💡 `range(5)` создаёт числа от `0` до `4`.

Можно проходить по списку:

```python
fruits = ["яблоко", "банан", "вишня"]

for fruit in fruits:
    print(fruit)
```

---

## 🔹 Цикл `while`

Выполняется, **пока условие истинно (True)**.

```python
count = 0

while count < 3:
    print("Счётчик:", count)
    count += 1
```

➡️ Выведет:

```
Счётчик: 0
Счётчик: 1
Счётчик: 2
```

💡 Чтобы остановить цикл:

```python
break     # прерывает цикл
continue  # пропускает шаг, но не останавливает
```

---

## 🔹 Функции (`def`)

Функции — это **повторно используемые куски кода**, которые можно вызывать когда нужно.

```python
def greet():
    print("Привет, Дарья!")
```

📞 Вызов функции:

```python
greet()
```

➡️ Вывод:

```
Привет, Дарья!
```

---

### 🔸 Функции с параметрами

Можно передавать данные в функцию.

```python
def greet_user(name):
    print(f"Привет, {name}!")
```

```python
greet_user("Дарья")
```

➡️ Вывод:

```
Привет, Дарья!
```

---

### 🔸 Функции с возвратом значения (`return`)

`return` возвращает результат из функции, чтобы использовать его дальше.

```python
def add(a, b):
    return a + b
```

```python
result = add(3, 5)
print(result)
```

➡️ Вывод:

```
8
```

---

### 🔸 Значения по умолчанию

Если аргумент не передан, используется значение по умолчанию.

```python
def greet(name="гость"):
    print(f"Привет, {name}!")
```

```python
greet()         # Привет, гость!
greet("Дарья")  # Привет, Дарья!
```

---

### 🔸 Локальные и глобальные переменные

* **Локальные** — видны только внутри функции
* **Глобальные** — доступны везде в программе

```python
x = 10  # глобальная

def show_number():
    x = 5  # локальная
    print("Внутри функции:", x)

show_number()
print("Снаружи функции:", x)
```

➡️ Вывод:

```
Внутри функции: 5
Снаружи функции: 10
```

---

## ✅ Краткая таблица

| Что делаем              | Команда             | Пример                |
| ----------------------- | ------------------- | --------------------- |
| Проверяем условие       | `if / elif / else`  | `if x > 0:`           |
| Повторяем N раз         | `for i in range(N)` | `for i in range(5):`  |
| Повторяем, пока условие | `while`             | `while x < 10:`       |
| Прерываем цикл          | `break`             | `if x == 3: break`    |
| Пропускаем шаг          | `continue`          | `if x == 2: continue` |
| Объявляем функцию       | `def`               | `def greet():`        |
| Передаём аргументы      | `(a, b)`            | `def add(a, b):`      |
| Возвращаем результат    | `return`            | `return a + b`        |

